/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['images.freeimages.com','headless-phukettourz.local'],
      },
}

module.exports = nextConfig
